mybatis作为dao层的传统开发模式（接口--实现类）

mybatis代理模式：开发人员遵循mybatis提供的一些规范的前提下，mybatis帮我们自动生成的dao接口的实现类

规范（规则）：
    1.映射文件中命名空间等于dao层接口的全路径
    2.映射文件中sql语句的id是dao层接口里的方法名
    3.映射文件中输入参数和输出参数的类型和dao层接口里的方法的参数和返回结果保持一致
